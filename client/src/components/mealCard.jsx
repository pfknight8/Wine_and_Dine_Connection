const MealCard = ({ meal, onClick }) => {
    // State
  // useEffects
  // Functions
  // Render (return)
  return (
    <div className="mealCard">
      <p>So...</p>
    </div>
  )
}

export default MealCard